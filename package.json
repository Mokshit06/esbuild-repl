{
  "name": "esbuild-repl",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "web:dev": "yarn workspace web start",
    "server:dev": "yarn workspace server dev",
    "start": "yarn workspace server start",
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build && mv packages/web/build packages/server/build"
  },
  "workspaces": [
    "packages/*"
  ]
}